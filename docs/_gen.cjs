const fs = require("fs");
const BQ = String.fromCharCode(96);
const CB = BQ+BQ+BQ;
const NL = String.fromCharCode(10);
let c = "";
c += "# Ariel 接口知识库 — 设计文档" + NL;
c += "" + NL;
c += "> 经过 3 轮 SubAgent 攻击验证，方案已通过。" + NL;
c += "" + NL;
c += "## 产品定位" + NL;
c += "" + NL;
c += "Ariel 的接口知识库是 **增强能力**，不是前提。即使知识库为空，Ariel 仍可通过 page_read + captured_api（会话级别）工作。知识库的价值在于让 Ariel **不依赖用户当前打开的页面**。" + NL;
c += "" + NL;
c += "## 核心架构：三层分离" + NL;
c += "" + NL;
c += CB + NL;
c += "LLM 上下文层  ← 只看精简接口目录（~200-500 token）" + NL;
c += "    │ 按需调用" + NL;
c += "工具调用层    ← 确定性的 invoke_api(recipe_id, params)" + NL;
c += "    │ 读取" + NL;
c += "知识库层      ← chrome.storage.local，按站点隔离" + NL;
c += CB + NL + NL;
